# Shell scripts run on Linux runners: a CRLF here makes bash fail on the shebang line.
*.sh text eol=lf

.github/workflows/*.lock.yml linguist-generated=true# touched to test the protected-files gate
