# Managed by @plainconceptsplatform/workflows@0.5.1. Source: loops/actions/verify-refine-output/action.yml. Profile digest: b7d3498150a7. Update with workflows update --force; consumer edits may be overwritten.
name: Verify refine output validation
description: Exercise deterministic Refine output validation regressions.
runs:
  using: composite
  steps:
    - name: Verify refinement outcomes
      shell: bash
      run: bash "${{ github.action_path }}/verify-refine-output.sh"
